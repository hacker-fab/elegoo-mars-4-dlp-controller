[project]
name = "elegoo-mars-4-dlp-controller"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "pyserial>=3.5",
    "pytest>=8.3.5",
]

[tool.ty.rules]
  possibly-missing-import = "ignore"
  unresolved-import = "ignore"

[tool.ruff]
  line-length = 240
